version: '3'
services:
  sharelatex:
    volumes:
      - '/var/lib/sharelatex:/var/lib/sharelatex'
  mongodb:
    volumes:
      - '/var/lib/mongodb/data:/data/db'
      - '/var/lib/backups/mongodb:/var/lib/backup'
  mysql:
    volumes:
      - '/var/lib/mysql:/var/lib/data'
      - '/var/lib/backups/mysql:/var/lib/backup'
  util:
    ports:
      - '22:22'
  plex:
    volumes:
      - /var/lib/plex:/config
