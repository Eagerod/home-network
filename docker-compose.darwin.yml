version: '3'
services:
  sharelatex:
    volumes:
      - 'sharelatex-data:/var/lib/sharelatex'
  openvpn-as:
    volumes:
      - 'openvpn-as-data:/config/etc/db'
      - '~/Desktop/docker/openvpn-ca:/var/lib/keys'
  openvpn-ca:
    volumes:
      - '~/Desktop/docker/openvpn-ca:/var/lib/openvpn-ca/keys'
  mongodb:
    volumes:
      - 'mongodb-data:/db/data'
      - '~/Desktop/docker/backups/mongodb:/var/lib/backup'
  mysql:
    volumes:
      - 'mysql-data:/var/lib/data'
      - '~/Desktop/docker/backups/mysql:/var/lib/backup'
  transmission-oss:
    volumes:
      - '~/Desktop/docker/downloads/oss:/var/lib/downloads'
      - '~/Desktop/docker/transmission-oss:/var/lib/transmission-daemon'
  util:
    volumes:
      - '~/Desktop/docker/backups:/var/lib/backups'
      - '~/Desktop/docker/git:/var/lib/git'
    ports:
      - '2000:22'
  resilio-server:
    volumes:
      - ~/Desktop/docker/resilio:/.sync
      - ~/Desktop/docker/backups:/var/lib/resilio-folders
  plex:
    volumes:
      - plex-config:/config

volumes:
  sharelatex-data:
    external: true
  mongodb-data:
    external: true
  mysql-data:
    external: true
  plex-config:
    external: true
  openvpn-as-data:
    external: true
